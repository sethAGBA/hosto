import 'package:flutter/material.dart';

class MenuItem {
  final IconData icon;
  final String title;
  final LinearGradient gradient;

  const MenuItem({
    required this.icon,
    required this.title,
    required this.gradient,
  });
}
